TelSeq is ....

This package contains:

	* a
	* b

After installation, tests can be run with:

python -c 'import telseq; telseq.test()'

The most current development version is always available from our
git repository:

gitrepository